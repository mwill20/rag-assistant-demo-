name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Cache HuggingFace
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/.cache/huggingface
          key: hf-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            hf-${{ runner.os }}-

      - name: Install project (editable) + dev + sbert
        env:
          HF_HOME: ${{ runner.temp }}/.cache/huggingface
        run: |
          python -m pip install -U pip
          pip install -e ".[dev]"
          pip install sentence-transformers

      - name: Prefetch embedding model (best-effort)
        env:
          HF_HOME: ${{ runner.temp }}/.cache/huggingface
        run: |
          python - << 'PY'
          try:
              from sentence_transformers import SentenceTransformer
              SentenceTransformer("sentence-transformers/all-MiniLM-L6-v2")
              print("Prefetch OK")
          except Exception as e:
              print("Prefetch warning:", e)
          PY

      - name: Run tests
        env:
          HF_HOME: ${{ runner.temp }}/.cache/huggingface
        run: pytest -q



